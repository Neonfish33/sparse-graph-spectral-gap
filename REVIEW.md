# Полный обзор проекта — наработки, выводы, что закрыто

Единый документ для перепроверки. Всё, что мы сделали, получили и почему
остановились. Ссылки на файлы/числа — чтобы можно было проверить независимо.

Дата итога: 2026-09-23. Репозиторий: https://github.com/Neonfish33/sparse-graph-spectral-gap

---

## 0. TL;DR (кратко)

- Строили теорию/численный анализ **спектральной щели λ₂ нормализованного
  лапласиана** разреженных случайных графов с тяжёлым хвостом степеней.
- Эмпирически получили чистые скейлинги (`λ₂ ~ n^{−α}` и т.д.).
- **Литпроверка показала: ядро уже опубликовано в 2003–2008.** Наш результат —
  **воспроизведение** (минимальная степень определяет край спектра; хвост вторичен;
  конечный размер λ₂(N) тоже известен).
- **Содержательной новизны нет.** Проект закрыт. Репо сохранён как инструмент/бенчмарк.
- Рассмотрели ~6 альтернативных направлений — **все тоже заняты** (см. §8).

---

## 1. Исходная задача

Получить асимптотику второй собственной величины `λ₂` нормализованного
лапласиана `L = I − D^{−1/2} A D^{−1/2}` для разреженных случайных графов с
`P(D=k) ∼ C k^{−γ}` в режиме `d̄_min = O(1)` — не покрытом теорией Chung–Lu–Vu.

Ансамбли: power-law (`d_min = 1, 2, 3`) и `planted` (модульность).

---

## 2. Что построено

### Код (`src/`, корень)
- `src/generators.py` — степень-последовательности, конфигурационная модель, 4 ансамбля.
- `src/diagnostics.py` — `λ₂` (full/giant/2-core), sweep cut, `φ_periph`, `φ_mod`,
  `φ*`, `Δ`, `ρ*`, IPR₂, φ-профиль.
- `run_experiments.py` — оркестрация (флаги `--jobs/--mu/--dmax/--resume/--save-graphs/...`).
- `analyze_results.py` — α(n), α(φ), фазовая диаграмма, режимы.
- `analyze_gap_fss.py` — finite-size scaling щели (`λ_c + C n^{−θ}`) и распределения.
- `validate_power_law.py` — AICc: степенной закон vs `1/log n` vs exp-log; bootstrap α.
- `run_er_control.py` — ER-контроль; `run_toy_controls.py` — хвост vs доля degree-2.
- `toy_model.py`, `merge_diagnostics.py`, `export_graph.py`.
- `tests/` — 19 тестов (спектр, sweep cut vs brute force, Cheeger).

### Данные
| Каталог | Что | В git? |
|---|---|---|
| `results_v2/` | 13500 графов, n=100…10⁴, γ=2.1/2.5/3.0, d_min=1/2/3, planted | ❌ (графы 231 МБ) |
| `results_large/` | n=3×10⁴ и 10⁵, d_min=2/3, reps=100 | диаграммы ✅ |
| `results_merged/` | v2+large, **14700 строк**, n=100…10⁵ | `diagnostics.csv` ✅ |
| `results_er/`, `results_toy/` | ER-контроль, toy-контроли | ✅ |
| `results_agents/` | отчёты независимых агентов | ✅ |

---

## 3. Эмпирические результаты (числа)

### 3.1. α в `λ₂(giant) ~ n^{−α}` (по n = 100…10⁵, 8 точек)
| ensemble | γ=2.1 | γ=2.5 | γ=3.0 |
|---|---|---|---|
| `pl_dmin1` | 0.268 | 0.401 | 1.100 |
| `pl_dmin2` | **0.150** (R²=1.00) | **0.158** (R²=1.00) | **0.171** (R²=1.00) |
| `pl_dmin3` | 0.037 | 0.035 | 0.029 |

- Для d_min=2 спад **степенной** на 3 декадах, насыщения нет (исход (b)).
- Локальный α на последней декаде (3×10⁴→10⁵): 0.145 / 0.133 / 0.183 — без систематического замедления.

### 3.2. Разрыв `λ₂(d3)/λ₂(d2)`
| γ | n=10³ | 10⁴ | 3×10⁴ | 10⁵ |
|---|---|---|---|---|
| 2.1 | 2.18 | 2.80 | 3.09 | **3.56** |
| 2.5 | 2.78 | 3.95 | 4.41 | **5.01** |
| 3.0 | 4.22 | 5.59 | 6.56 | **7.95** |

### 3.3. Cheeger
`α_φ` (λ₂ ~ (φ*)^α) ∈ [1, 2] для d2: 1.41 / 1.45 / 1.52; для d3 степенного закона нет.

### 3.4. Локализация (ключевое)
`IPR₂(giant) ≈ const` (0.07–0.19) при росте n ⇒ **λ₂ локализован на ~6–11 вершинах**,
носитель — цепочка вершин степени 2 (проверено dense-пересчётом агентом-скептиком).

### 3.5. Доля degree-2
`P(2) = N₂/n` для d2: 0.426 / 0.520 / 0.619 (γ=2.1/2.5/3.0); для d3: ≈0.003.
`q = 2·P(2)/d̄`.

### 3.6. Комбинаторный vs нормализованный лапласиан (`compare_laplacians.py`)
На **одних и тех же** сохранённых графах (pl_dmin2, n=100…10⁴, 10 reps):

| γ | наблюдаемая | α (степень) | R² | β (log) | R² |
|---|---|---|---|---|---|
| 2.1 | norm | 0.151 | 0.963 | 1.01 | 0.964 |
| 2.1 | **comb** | 0.186 | 0.969 | 1.24 | 0.980 |
| 2.5 | norm | 0.151 | 0.890 | 1.00 | 0.885 |
| 2.5 | comb | 0.183 | 0.895 | 1.22 | 0.902 |
| 3.0 | norm | 0.192 | 0.976 | 1.28 | 0.984 |
| 3.0 | comb | 0.212 | 0.977 | 1.42 | 0.982 |

**Выводы:**
- Комбинаторный лапласиан спадает **так же** (степенно-подобно), как нормализованный ⇒
  «нормализованный vs комбинаторный» — **не** лакуна; явление одно.
- Степенной закон и `(ln N)^{−2}` на n≤10⁴ **неразличимы** (R² отличаются на ~0.001–0.01).
- ⇒ асимптотика 2008 `(ln N)^{−2}` на нашем диапазоне **не видна** (либо включается при n≫10⁵).

### 3.7. Асимптотическая проверка до n=10⁶ (`compare_asymptotics.py`)
Добавлены n=3×10⁵ и 10⁶ (pl_dmin2/3, γ=2.5/3.0, reps=5) в `results_asymptotic/`,
совмещено с основным датасетом (n=100…10⁵). Полный диапазон n=100…10⁶.

| ensemble/γ | α(≤10⁵) | α(≤10⁶) | R²(power) | R²(log) | лучше |
|---|---|---|---|---|---|
| d2 γ=2.5 | 0.158 | 0.147 | 0.989 | 0.978 | power |
| d2 γ=3.0 | 0.171 | 0.157 | 0.990 | 0.995 | **log** |
| d3 γ=2.5 | 0.035 | 0.032 | 0.987 | 0.994 | **log** |
| d3 γ=3.0 | 0.030 | 0.026 | 0.958 | 0.991 | **log** |

Локальные наклоны (log-log) на больших n:
- d2 γ=2.5: 10⁵→3×10⁵ = **0.015**, 3×10⁵→10⁶ = **0.242** (шумно: reps=5);
- d2 γ=3.0: 0.108 → 0.086 (снижается);
- d3 γ=2.5/3.0: ~0.02–0.04 → ~0.007–0.021 (плато).

**Вывод:** при расширении до n=10⁶ α **снижается** у всех наборов, а логарифмическая
форма `A(ln n)^{−β}` (β≈1.2–1.33) становится предпочтительнее в **3 из 4** случаев.
Это **согласуется** с асимптотикой 2008 `(ln N)^{−2}` — спад **замедляется** к
логарифму. Оговорка: β<2 (ещё не точная `(ln N)^{−2}`), и при n=10⁶ всего 5
реализаций → шумно; переход очень медленный.

**Итог:** ядро 2008 (min degree → край) **подтверждено**; конечный α — эффективный,
асимптотика стремится к логарифмической. **Новизны нет**, но картина замкнута.

---

## 4. Проверка независимыми агентами

### 4.1. Агент-скептик (`results_agents/devils_advocate/report.md`)
Опровергнуто (низкая серьёзность): джиттер n, смена eigensolver, слияние v2+large, несвязность.
Опасное:
- **(#5/#6) Сравнение PL vs ER — склейка разных объектов.** Мы сравнивали
  `λ₂(giant)` PL с `λ₂(core)` ER. Like-for-like:
  - **giant vs giant:** PL 0.15–0.17 **меньше** ER 0.17–0.27 → PL *медленнее*;
  - **core vs core:** 1.5–2.1×.
  ⇒ «PL в 2× быстрее ER» — **артефакт**.
- **(#10) λ₂ — локализованный дефект**, а не глобальная щель (IPR·n линейно растёт).
- **(#11) Каузальный вывод «хвост» конфаундирован** долей degree-2 (d2→d3 переход
  совпадает с обнулением P(2)).
- **(#7/#8) R²≈1.0 — эффект усреднения**; AICc для d2 γ3.0 и d3 выбирает exp-log.

### 4.2. FSS-агент (`results_agents/fss/report.md`)
- Чистый степенной закон **проходит** для: d2 γ=2.1 (0.150), γ=2.5 (0.158),
  ER c=3 (0.206), c=4 (0.168), d3 γ=2.1 (маргинально).
- **Отвергнут** для: d2 γ=3.0 (`exp(−c(log n)^β)`), d3 γ=2.5 и 3.0 (`(log n)^{−β}`
  / плато), ER c=2.
- **Насыщение** (ненулевой флор C≈0.16) у d3 γ=2.5, 3.0.
- α дрейфует с диапазоном n для «отвергнутых» наборов ⇒ эффективный, не асимптотический.

---

## 5. Контроли

### 5.1. ER-контроль (`run_er_control.py`)
| модель | core d̄ | α(core) | α(giant) |
|---|---|---|---|
| ER c=2 | 2.68 | 0.138 | 0.270 |
| ER c=3 | 3.44 | 0.089 | 0.206 |
| ER c=4 | 4.26 | 0.072 | 0.168 |

Вывод: **качественный спад генерический** (ER тоже затухает) ⇒ не power-law-специфичен.

### 5.2. Toy-контроли (`run_toy_controls.py`)
| семейство | хвост | α_core |
|---|---|---|
| uniform {2..D}, D=3..20 | компактный | **0.06–0.08 (плоско)** |
| power-law {2..D}, D=3..50 | тяжёлый | 0.096 → 0.164 |
| {2,3}, p2=0.4..0.8 | — | 0.045 → 0.092 |
| {2,3,4} | — | 0.094 / 0.098 |

Вывод: гипотеза «хабов» (скачок при max degree ≥5) **опровергнута**; но и α **не**
функция (d̄, P(2), max degree, ветвистости). Решает форма хвоста — но это не подтвердилось
однозначно (см. §4.1 #11).

---

## 6. Литература: почему новизны нет (разбор в `LITERATURE.md`)

🔴 **Ключевая работа — Samukhin, Dorogovtsev, Mendes, PRE 77, 036115 (2008)**
(arXiv:0706.1176), *«Are scale-free architectures really important?»*:
- нижний край спектра `λ_c` определяется **минимальной степенью** `q_m`;
- **`λ_c > 0` при `q_m > 2`, `λ_c = 0` при `q_m ≤ 2`** ⇒ наша дихотомия d2/d3;
- «high-degree part … is not that essential» ⇒ наш вывод «хвост вторичен»;
- **конечный размер:** `λ₂(N) ∼ (ln N)^{−2}` при `q_m ≤ 2` и
  `λ₂(N) − λ_c ∼ (ln ln N)^{−2}` при `q_m > 2` ⇒ наш «power law» асимптотически логарифмический;
- нормализованный лапласиан предвиден («main conclusions are also valid if the
  escape rate from a vertex is fixed»).

Дополнительно:
- **DGMS, PRE 68, 046109 (2003)** — спектр локально treelike сетей, `ρ(λ) ∼ |λ|^{1−2γ}`, роль малых степеней.
- **ISCCSP 2012** (10.1109/isccsp.2012.6217860) — нулевая щель норм. лапласиана ER-giant.
- **Hata & Nakao, Sci Rep 7, 1121 (2017)** — локализация лапласиан-векторов (плотный режим).

**Вывод:** ядро, конечный размер и качественная локализация — уже опубликованы (2003–2008).

---

## 7. Что НЕ подтвердилось (опровергнутые интерпретации)

1. «Power-law в ~2× быстрее ER» — артефакт непарного сравнения (giant vs core).
2. Каузальное «тяжёлый хвост ускоряет спад» — конфаунд с долей degree-2.
3. «Чистый универсальный степенной закон» — верно только для d2 γ=2.1, 2.5.
4. «λ₂ — глобальная спектральная щель» — на деле локализованный дефект.
5. Мой прогноз «насыщение (a)» — не подтвердился (исход (b) на доступном диапазоне).
6. Гипотеза «хабов» (max degree) — опровергнута.
7. Первое объяснение «bounded degrees ⇒ экспандер» — не работает при d_min≤2.

---

## 8. Рассмотренные пивоты — все заняты

| Направление | Статус |
|---|---|
| Гиперграфовый SBM / пороги | занято (Pal–Zhu 2021, Stephan–Zhu 2022/24, Fernandez–Stephan–Zhu 2026) |
| Делокализация лапласиан-векторов | занято (Huang–Landon 2015, Metz–Neri PRL 2021, García-Mata и др.) |
| Temporal × spectral | маленькое поле; якоря 2013 (Masuda) + 2024–2026; **одна щель — неэрмитова ветвь** |
| Distributed learning / mesh + TinyML | занято (RockNet 2025, AIRPLAN 2026, DFL-топология) |
| Биология (коннектомы/Hi-C/GRN/PPI) | занято, конкуренция системных/ML-групп |
| Гача/loot boxes | занято психологией; возможна ниша в **математике pity-механик** |

---

## 9. Что потенциально остаётся открытым (честно, без гарантий)

1. **Неэрмитова спектральная теория time-respecting блужданий** — запрос
   «non-Hermitian + temporal network» даёт ~0 работ. Требует глубокого погружения.
2. **Спектральная размерность** time-respecting блужданий (`p(t) ~ t^{−d_s/2}`).
3. **Локализация временных мод** (собственные векторы temporal-операторов).
4. **Математика pity-систем в гаче** (марковские цепи/renewal/hitting times) — возможно, не занята.

Все — узкие и рискованные; ни одна не гарантирует «крупный журнал» без кооперации.

---

## 10. Как перепроверить самому

### Ключевые числа
- `results_merged/analysis/scaling_by_n.csv` — α(n) по всем ансамблям.
- `results_merged/analysis/core_degree_comparison.csv` — `ratio_d3_over_d2` по n.
- `results_merged/analysis/scaling_by_phi.csv` — α_φ (Cheeger).
- `results_agents/devils_advocate/report.md`, `results_agents/fss/report.md` — критика.

### Команды
```bash
python analyze_results.py --csv results_merged/diagnostics.csv --out results_merged/analysis
python validate_power_law.py          # AICc + bootstrap α
python analyze_gap_fss.py             # finite-size λ_c, θ, распределения
python -m pytest tests/ -q            # тесты
```

### Что стоит перепроверить в первую очередь (если искать упущенное)
1. **Действительно ли 2008 покрывает нормализованный лапласиан** (у них комбинаторный;
   они лишь заявили перенос). Открой PRE 77, 036115 и проверь §§ про «fixed escape rate».
2. **Не является ли α=0.15 конечномерным** (их `(ln N)^{−2}`): нужны n≫10⁵.
3. **Неэрмитова ветвь temporal** — точно ли пуста (поиск шире: pseudospectra,
   non-normal, exceptional points).

---

## 11. Итог

- **Технически**: построен полный, воспроизводимый спектральный пайплайн + данные + тесты.
- **Научно**: содержательной новизны нет — воспроизведён известный результат (2003–2008).
- **Правильный вывод**: закрыть тему (сделано) и не идти в переполненные ниши.
- **Что осталось**: либо узкая неэрмитова/спектральная temporal-тема (риск, нужна
  кооперация), либо артефакт/инженерия, либо другой домен (например, математика гачи).

---

*Если при перепроверке найдёшь что-то, что мы упустили — пришли: разберём точечно.*
