# Журнал изменений проекта

> 🌐 English version: [`CHANGELOG.en.md`](CHANGELOG.en.md)

Проект: спектральная щель нормализованного лапласиана в разреженных
случайных графах с тяжёлохвостыми степенями.

---

## 2026-09-23 — Литпроверка: ядро переоткрывает PRE 2008; смена вопроса

Чтение ключевых работ показало, что центральный качественный результат **уже
известен**:

- **Samukhin, Dorogovtsev, Mendes**, *Are scale-free architectures really
  important?*, **PRE 77, 036115 (2008)**: нижний край спектра лапласиана
  определяется **минимальной степенью**; `λ_c>0` при `q_m>2`, `λ_c=0` при
  `q_m≤2`; хвост степеней не существенен. ⇒ **наша дихотомия d_min=2/3 и вывод
  «хвост вторичен» — воспроизведение известного результата.**
- **DGMS**, *Spectra of complex networks*, **PRE 68, 046109 (2003)**: спектр
  локально treelike сетей, `ρ(λ)∼|λ|^{1−2γ}`, роль малых степеней.
- **Hata & Nakao**, **Sci Rep 7, 1121 (2017)**: локализация лапласиан-векторов
  (degree–eigenvalue correspondence), но в **плотном** режиме `⟨k⟩≫1`.

**Итог:** «новый закон» снимается. Проект переориентируется на вопрос **за
пределами 2003–2008**: finite-size scaling и флуктуации щели у перехода
`q_m=2→3` (направление A). Литпроверка A прямых совпадений **не нашла**
(finite-size scaling спектральной щели у min-degree-перехода, предварительно,
не опубликован).

Предыдущие результаты (v2, large, ER, toy-контроли) сохраняют ценность как
воспроизведение/численная детализация известного **и как данные для A**.

---

## 2026-09-23 — Этап 3a: toy-контроли (хвост vs доля degree-2)

`run_toy_controls.py`, `results_toy/` (gitignore). Семейства: `pl{γ=2.5,D}`,
`uniform {2..D}`, `{2,3}` и `{2,3,4}`; n = 10³,10⁴,10⁵, reps = 20.

| семейство | хвост | α_core |
|---|---|---|
| uniform {2..D}, D=3..20 | компактный | **0.06–0.08 (плоско)** по D, d̄, q |
| power-law {2..D}, D=3..50 | тяжёлый | 0.096 → **0.164** |
| {2,3}, p2=0.4..0.8 | — | 0.045 → 0.092 |
| {2,3,4} | — | 0.094 / 0.098 |

**Выводы:**
- Гипотеза «хабов» (скачок при max degree ≥5) **опровергнута**: uniform α≈0.07
  при D до 20, включая d̄=11.
- α **не** функция от (d̄, P(2), q, max degree, ветвистости ρ). Uniform D=20
  (d̄=11, ρ≈13) даёт α=0.064, а ER c=2 (d̄=2.7) даёт α=0.138.
- Решает **форма хвоста**: тяжёлый/неограниченный хвост усиливает α ~2× против
  компактного. Доля degree-2 (гипотеза B) сама по себе не объясняет (q меняется
  в 40 раз, α плоско).
- Правильный структурный параметр не найден → задача для toy model.

### Toy model подготовлен на завтра

- `TOY_MODEL.md` — дизайн: H1 (α = 1/β, `F(λ)~λ^β`), H2 (длины цепочек степени 2),
  H3 (backbone), кроссовер exp-хвоста; критерий успеха.
- `toy_model.py` — скелет (low-eig плотность, β, длины цепочек, модели unif/pl/
  two23/er/expo), CLI `--quick`.
- `run_toy_model.bat` — запуск.
- Быстрый прогон: H1 в наивной форме (fit β по всем K) **не совпал** с α —
  уточнять (фит только у 0, больше n).

---

## 2026-09-23 — Этапы 0/1/1.5: проверка закона, литпоиск, ER-контроль

### Этап 0 — `validate_power_law.py` (новый)

Проверка на `results_merged/diagnostics.csv`: AICc (power law vs `A/log n`
vs `A·exp(−c·(log n)^β)`), bootstrap CI для α (ресэмпл реализаций внутри n),
локальный α по декадам. **Важно:** фильтровать по ensemble (`pl_dmin*`), иначе
planted-ансамбли портят α (первая версия давала 0.117 вместо 0.150).

Результаты (λ₂_giant):

| d_min | γ | α (log-fit) | R² | bootstrap 95% CI | победитель AICc |
|---|---|---|---|---|---|
| 2 | 2.1 | 0.1496 | 0.993 | [0.1433, 0.1561] | power_law |
| 2 | 2.5 | 0.1580 | 0.996 | [0.1493, 0.1664] | power_law |
| 2 | 3.0 | 0.1708 | 0.994 | [0.1612, 0.1796] | **exp-log** (ΔAICc≈10) |
| 3 | 2.1 | 0.0374 | 0.994 | [0.0348, 0.0399] | power_law |
| 3 | 2.5 | 0.0345 | 0.990 | [0.0316, 0.0373] | exp-log |
| 3 | 3.0 | 0.0295 | 0.953 | [0.0262, 0.0326] | exp-log |

Вердикт: согласуется со степенным законом (α совпал с `analyze_results`), α
растёт с γ (CI 2.1 и 3.0 не пересекаются), насыщения нет. **Но** для γ=3.0 (d2)
и для d3 лучше подходит `exp(−c·(log n)^β)` (β≈0.5, медленнее любого
степенного) — «чистый степенной закон» не однозначен.

### Этап 1 — литпоиск (подробно в `LITERATURE.md`)

arXiv API + Crossref + OpenAlex. Точного скейлинга не найдено. **Критично:**
найден прецедент — normalized Laplacian giant **разреженного ER** имеет нулевую
щель при n→∞ (ISCCSP 2012, 10.1109/isccsp.2012.6217860). То есть *качественный*
спад не нов.

### Этап 1.5 — ER-контроль (`run_er_control.py`, `results_er/`)

ER `G(n, c/n)`, c ∈ {2,3,4}, n = 100…10⁵, reps = 20 → 480 графов (~10 мин,
`--jobs 6`). `results_er/` — на диске (gitignore).

| модель | core d̄ | α(λ₂_core) | α(λ₂_giant) |
|---|---|---|---|
| ER c=2 | 2.68 | 0.138 | 0.270 |
| ER c=3 | 3.44 | 0.089 | 0.206 |
| ER c=4 | 4.26 | 0.072 | 0.168 |
| pl_dmin2 γ=3.0 | 3.08 | 0.171 | — |
| pl_dmin2 γ=2.5 | 3.87 | 0.158 | — |
| pl_dmin2 γ=2.1 | 5.06 | 0.150 | — |
| pl_dmin3 | 4.8–7.3 | 0.029–0.037 | — |

**Вердикт:** (1) качественный спад — **генерический** (ER тоже даёт n^(−α)),
не power-law; (2) при равной core d̄ у power-law α **~2× больше** ER
(≈0.16 vs ≈0.09 при d̄≈3.5) → хвост/структура степеней добавляет; (3)
**конфаунд**: у pl_dmin2 большая доля вершин степени 2, у ER-core — нет; эффект
может быть от доли degree-2, а не от хвоста. Переход d2→d3 резкий (0.15→0.03).

### Следующее

Контроли с ограниченным носителем: **M_b** степени ∈ {2,3} (варьируем долю p₂,
без хвоста) и **M_a** степени ∈ {2..D} (вар. D = тест d_max). Разделить «хвост»
и «долю degree-2». Это и есть численный toy model.

---

## 2026-09-23 — v2 полный прогон, диагностическая система, исправления

### Данные (не в git, лежат на диске)

| Каталог | Содержимое |
|---|---|
| `results_v2/` | **13500 графов**: n = 100…10000, γ = 2.1/2.5/3.0, d_min = 1/2/3, planted d_min = 1/2/3, μ = 0.05/0.1/0.2/0.3, d_max = 50, reps = 50 |
| `results_v2/graphs/` | все графы, `.npz` (13500 файлов, **230.7 МБ**), узлы 0..n−1, для planted — метки |
| `results_v2/analysis/` | `core_degree_comparison.csv`, `scaling_by_n.csv`, `scaling_by_phi.csv`, `phase.csv`, `summary.txt`, 3 фигуры |
| `results_fixed/` | предыдущий прогон (6300 графов, planted только d_min=1) |
| `results_large/` | **тест больших n**: n = 3×10⁴ и 10⁵, γ = 2.1/2.5/3.0, d_min = 2/3, без planted, reps = 100 → 1200 графов; `phi_curves.csv` есть |
| `results_merged/` | `results_v2` + `results_large` (**14700 строк**, n = 100…10⁵), `n_target` восстановлен для v2; `analysis/` финальные таблицы |

### Код

- `src/diagnostics.py` — добавлены `ipr`, `ipr_pi` (π-взвешенный IPR),
  `sweep_curve`, `graph_sweep_curve`; IPR₂/PR₂ в метрики; **исправлен баг
  `φ* = 0`** (обновление global-минимума на `S = V`); корректная обработка
  несвязных графов (λ₂ = 0, sweep → nan); nan-логика `log Δ`.
- `src/generators.py` — `planted_d_mins`, `graph_id`, `save_graph`, `load_graph`.
- `run_experiments.py` — флаги `--jobs`, `--mu` (несколько), `--dmax`,
  `--save-curves`, `--save-graphs`, `--resume`, `--no-planted`,
  `--planted-dmin`; колонка `n_target`; исправлена **дублирующаяся колонка
  `d_min`** (observed → `d_min_obs`).
- `analyze_results.py` — α(n) и α(φ) с R² **внутри (ensemble, γ)** (без
  пулинга); `core_degree_comparison`; фазовая диаграмма; `n_target`.
- `export_graph.py` — экспорт `.npz` → edgelist / CSV / GraphML / GML / JSON.
- `run_large.bat` — запуск теста больших n (3×10⁴, 10⁵).
- `tests/test_spectral.py` (6 тестов), `tests/test_known_spectra.py` (13) — все проходят.

### Ключевые эмпирические результаты (конечные n)

1. **Три режима** по α в `λ₂(giant) ~ n^(−α)`:
   d_min=1 (периферия) α = 0.27…0.97 (растёт с γ); d_min=2 α ≈ 0.14…0.19;
   d_min=3 α ≈ 0.03…0.04 (λ₂ ≈ const).
2. **Matched d_min=2 vs d_min=3** (оба без листьев), разрыв `λ₂(d3)/λ₂(d2)`
   растёт с n: γ=2.1 1.68→3.56; γ=2.5 2.22→5.01; γ=3.0 2.96→7.95 (n=100→10⁵,
   см. раздел «Большой прогон» ниже).
3. **Cheeger:** α_φ ∈ [1,2] для периферии, d_min=2 и planted; для d_min=3
   степенного закона нет (λ₂ ≈ const).
4. **Planted:** модульный сигнал виден в core (λ₂(core) растёт с μ); с d_min ≥ 2
   planted-графы связны.

### Исправленные ошибки

- `φ* = 0` и `ρ* = 1` из-за тривиального `S = V` (портило все прежние φ*/ρ*).
- Дублирующаяся колонка `d_min` в CSV.
- α считался по фактическому n (джиттер ±единицы) → по целевому `n_target`.
- Ранее заявленный «α_φ = 1.90 для core» — **артефакт пулинга** (внутри групп 1.19–1.59).
- Ложное «исправление» sweep cut из внешнего ревью **отклонено** (мой код совпал с brute force).

### Большой прогон n = 3×10⁴, 10⁵ — РЕЗУЛЬТАТ (исход (b))

Прогон завершён 2026-09-23 (~2.5 ч, `--jobs 6`), 1200/1200 графов.

**Вердикт: степенной спад λ₂(d_min=2) продолжается, насыщения нет. Исход (b),
прогноз (a) не подтвердился.**

α в λ₂(giant) ~ n^(−α) по n = 100…10⁵ (8 точек, `results_merged/analysis/scaling_by_n.csv`):

| ensemble | γ=2.1 | γ=2.5 | γ=3.0 |
|---|---|---|---|
| pl_dmin2 | 0.150 (R²=1.00) | 0.158 (R²=1.00) | 0.171 (R²=1.00) |
| pl_dmin3 | 0.037 (R²=0.99) | 0.035 (R²=0.99) | 0.029 (R²=0.96) |

R² = 1.00 на трёх декадах ⇒ точки 10⁵ идеально легли на прямую, кривизны нет.
Локальный α(λ₂ d2) на декаде 3×10⁴→10⁵: 0.145 / 0.133 / 0.183 против глобальных
0.150 / 0.158 / 0.171 — систематического замедления нет.

`ratio = λ₂(d3)/λ₂(d2)` (n = 100…10⁵):

| γ | 10³ | 10⁴ | 3×10⁴ | 10⁵ |
|---|---|---|---|---|
| 2.1 | 2.18 | 2.80 | 3.09 | **3.56** |
| 2.5 | 2.78 | 3.95 | 4.41 | **5.01** |
| 3.0 | 4.22 | 5.59 | 6.56 | **7.95** |

Прогноз был: γ=2.1 → 3.0–3.6 (факт 3.56 ✓); γ=2.5 → 4.2–5.0 (факт 5.01, чуть
выше); γ=3.0 → 5.8–7.0 (факт 7.95, выше; чистый закон дал бы 7.88).

Cheeger сохранён: α_φ(pl_dmin2) = 1.41 / 1.45 / 1.52 (все в [1,2]).

**Смысл:** аргумент «bounded degrees (d_max=50) ⇒ экспандер ⇒ λ₂ = Θ(1)» для
heavy-tail + min degree 2 **не работает**. Спад n^(−0.15…−0.17) — устойчивый
результат, а не конечный размер. Это центральный вопрос к toy model.

### Следующие шаги

1. Toy model: объяснить **степенной** спад при bounded степенях (не насыщение) —
   non-backtracking / Alon–Boppana здесь не даёт `n^(−α)`; искать механизм
   (вероятно, рост длин цепочек степени 2 или хвост степеней).
2. Восстановить `n_target` для v2 надёжнее (сейчас — округление к целевой цели);
   в идеале — перегенерировать v2 текущим кодом.
3. Раздел Results в `paper_en.tex` + `paper.tex` с исходом (b); честные limitations.
   *(неактуально: статьи удалены, проект закрыт — см. `LITERATURE.md`)*
4. (Опционально) blind validation, реальные сети.

---

## Ранняя история (кратко)

- 2026-09-21: первичный код (generators/diagnostics/run_experiments), конспект
  `README.md`, `paper.tex`/`paper.pdf`, английская версия `paper_en.tex`/`paper_en.pdf`.
  **Позже (2026-09-23) статьи удалены** — их основное утверждение оказалось
  воспроизведением известного результата (PRE 2008), см. `LITERATURE.md`.
- Внешний архив `files.zip` с «исправлениями» разобран: 2 из 3 критических
  «багов» оказались ложными, но найден реальный баг `φ* = 0`.
