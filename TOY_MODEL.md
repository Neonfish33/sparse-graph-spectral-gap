# Toy model — план на завтра

Цель: **объяснить показатель α** в `λ₂(giant) ~ n^(−α)` через параметры
распределения степеней — найти правильный структурный параметр и вывести α.

---

## 0. Жёсткие ограничения из экспериментов (не переоткрывать!)

| семейство | хвост | α_core |
|---|---|---|
| uniform {2..D} | компактный | **0.06–0.08 (плоско)** по D=3..20, d̄=2.5..11, q=0.40..0.01 |
| power-law {2..D} | тяжёлый | 0.096 (D=3) → **0.164** (D=50) |
| ER G(n,c/n) | Poisson | 0.072 (c=4) → **0.138** (c=2) |
| ER giant (с листьями) | Poisson | 0.168 (c=4) → 0.270 (c=2) |

**Установлено:** α **не** функция от (d̄, P(2), max degree, ветвистости
ρ=E[D(D−1)]/E[D]). Uniform D=20 (d̄=11, ρ≈13) даёт α=0.064, а ER c=2 (d̄=2.7)
даёт α=0.138. ⇒ Решает **форма хвоста** (неограниченность/тяжесть), не моменты.
Гипотеза «хабов» (скачок при max degree ≥5) **опровергнута**.

## 1. Гипотезы

- **H1 (спектральная плотность).** `λ₂` — край конечномерного хвоста плотности
  малых собственных значений. Если интегрированная плотность у 0 ведёт себя как
  `F(λ) ~ λ^β`, то `λ₂ ~ n^(−1/β)`, т.е. **α = 1/β**. Здесь β — «спектральная
  размерность». Проверяемо численно: измерить β и сравнить α с 1/β.
- **H2 (цепочки степени 2).** α определяется распределением длин максимальных
  цепочек вершин степени 2 (экстремальная длина ℓ_max, хвост P(ℓ>L)).
- **H3 (backbone).** После подавления цепочек степени 2 получается мультиграф с
  сопротивлениями (≈ длины цепочек); его спектральная щель связана с α.

## 2. Численные эксперименты (запускаются `toy_model.py`)

1. **H1:** для каждой модели при фиксированном n=10⁴ измерить K=150 наименьших
   собственных чисел giant, фитировать `log F(λ)` vs `log λ` → β, затем α_pred=1/β.
   Сверить с известными α (unif≈0.07, pl≈0.16, ER≈0.09–0.14).
2. **H2:** распределение длин цепочек степени 2: mean, max, P(ℓ>L). Тяжёлый хвост
   у PL, геометрическое у uniform?
3. **H3:** подавленный мультиграф с весами = длины цепочек; λ₂ и его зависимость.
4. **Кроссовер:** экспоненциальный хвост `P(k) ∝ e^{−θk}` на [2,∞), варьируем θ:
   при θ→∞ компактный (α≈0.07), при θ→0 тяжёлый (α↑). Найти точку перехода.

## 3. Аналитика (цель)

Вывести β (или ℓ-статистику) из производящей функции распределения степеней /
локального слабого предела (unimodular Galton–Watson дерево) и получить
`α = F({p_k})`. Ориентиры в литературе: Hermon–Li–Yao–Zhang (min degree ≥3 ⇒ Θ(1)),
Chung–Lu–Vu (d̄_min ≫ ln²n), «spectral dimension» (Avrachenkov et al.).

## 4. Критерий успеха

Найдена одна функция φ(распределение) такая, что `α ≈ φ` одновременно для
uniform (0.07), power-law (0.10–0.16) и ER (0.07–0.14). Тогда toy model объясняет
и генерический фон (B), и хвостовое усиление (A).

## 5. Файлы

- `toy_model.py` — скелет (H1+H2, заготовки H3/H4), CLI.
- `run_toy_model.bat` — быстрый запуск.
- Результаты → `results_toy_model/`.

Запуск (быстрая проверка):
```
python toy_model.py --quick
python toy_model.py --n 10000 --K 150 --out results_toy_model
```
